# Global Configuration
environment: development
debug: true

# Logging
logging:
  level: INFO
  file: logs/agent_system.log

# Agent Defaults
agents:
  default_timeout: 30
  default_retries: 3
  max_concurrent: 10

# Graph
graph:
  max_iterations: 100
  execution_timeout: 300