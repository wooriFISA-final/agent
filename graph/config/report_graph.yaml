entry_point: report_main_builder

nodes:
  # 통합 보고서 작성 노드
  - name: report_main_builder
    # 🚨 [변경됨]: agent: report_main_agent -> report_agent
    agent: report_agent
    config:
      max_iterations: 20
      timeout: 480 # 8분
      
edges:
  # 최종 완료 (DynamicRouter)
  - from: report_main_builder
    router: DynamicRouter
    paths:
      REPORT_COMPLETE: __end__
      TOOL_FAIL: __end__